application: diris-app
version: 0-0-3
runtime: python27
api_version: 1
threadsafe: true

builtins:
- appstats: on

handlers:
- url: /_ah/(mapreduce|queue|warmup|start|stop).*
  script: diris.wsgi.application
  login: admin
  secure: always

- url: /_ah/internalupload.*
  script: diris.wsgi.application
  secure: always

- url: /static
  static_dir: static/
  secure: always

- url: /(.*\.(html|js|css|png|json|map|ico)|.*glyphicons-halflings-regular.*)$
  static_files: static/\1
  upload: static/(.*\.(html|js|css|png|json|map|ico)|.*glyphicons-halflings-regular.*)$
  secure: always

- url: /$
  static_files: static/index.html
  upload: static/index.html$
  secure: always

# Set Django admin to be login:admin as well as Django's is_staff restriction
- url: /admin.*
  script: diris.wsgi.application
  secure: always
  login: admin

- url: /.*
  script: diris.wsgi.application
  secure: always

skip_files:
  - ^(.*/)?#.*#$
  - ^(.*/)?.*~$
  - ^(.*/)?.*\.py[co]$
  - ^(.*/)?.*/RCS/.*$
  - ^(.*/)?\..*$
  - ^(.*/)?manage\.py$
  - ^(.*/)?README.md
  - ^(.*/)?install_deps$
  - ^(.*/)?pip-selfcheck.json$
  - ^(.*/)?requirements.*$
  - ^(.*/)?bin/.*$
  - ^(.*/)?include/.*$
  - ^/?lib/.*$
  - ^(.*/)?share/.*$
  - ^(.*/)?man/.*$
  - ^(.*/)?sitepackages/dev.*$
  - ^(.*/)?\.storage.*$
  - ^(.*/)?\.git.*$
